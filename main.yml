- name: Set up Crawler
        uses: actions/setup-java@v1
        with:
          java-version: '1.8'
      - name: Publish package
        run: mvn -B deploy
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
